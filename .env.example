# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenRouter AI (required for Website Builder and AI features)
OPENROUTER_API_KEY=sk-or-...

# Twilio SMS (optional — SMS notifications)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_PHONE_NUMBER=+18015550100
NOTIFY_PHONE_NUMBER=+18015550100

# Google Analytics 4
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# App URLs
NEXT_PUBLIC_BACKEND_URL=https://app.useezly.com
NEXT_PUBLIC_SITE_URL=https://app.useezly.com

# Prolink Sales CRM
# RESEND_API_KEY is shared with the /api/contact route.
# CRM_FROM_EMAIL must use a domain verified in Resend before deliveries
# outside your test inbox will succeed.
RESEND_API_KEY=re_...
CRM_FROM_EMAIL=Brian Nelsen <Brian@useezly.com>
# Optional: where recipient replies should land. If unset, replies go to
# the From address above. Set this if you want replies routed to a shared
# inbox or a different person from the From identity.
# CRM_REPLY_TO=sales@useezly.com

# REQUIRED before sending mass-email campaigns. CAN-SPAM mandates a
# physical mailing address in every cold-outreach email footer. P.O. Box
# is acceptable. Sends will refuse with a 400 if this is unset.
CRM_PHYSICAL_ADDRESS=Prolink, PO Box 0000, Salt Lake City, UT 84101

# Resend webhook signing secret. Configure in the Resend dashboard
# (Webhooks → Add endpoint → URL = https://app.useezly.com/api/webhooks/resend),
# then copy the signing secret here. Without this set, the webhook
# endpoint 401s every request — so opens / bounces won't be recorded.
RESEND_WEBHOOK_SECRET=whsec_...

# SignWell e-signature (scaffolded — keep disabled until ready to activate)
SIGNWELL_ENABLED=false
SIGNWELL_API_KEY=
SIGNWELL_WEBHOOK_SECRET=
NEXT_PUBLIC_SIGNWELL_ENABLED=false

# ── Arcads API (skill pack) ───────────────────────────────────────────────────
# Run ./scripts/setup.sh for guided setup, or fill in manually.
# Option A (recommended): Pre-encoded Basic auth header from Arcads dashboard.
ARCADS_BASIC_AUTH='Basic your_base64_encoded_credentials_here'
# Option B: Raw API key (used as HTTP Basic username with empty password).
# ARCADS_API_KEY=your_key_here

# ── Meta Ads (meta-ad-builder skill — OPTIONAL) ───────────────────────────────
# META_ACCESS_TOKEN=your_long_lived_token_here
# META_AD_ACCOUNT_ID=act_1234567890
# META_PAGE_ID=
# META_IG_USER_ID=
# META_PIXEL_ID=
# META_API_VERSION=v23.0
